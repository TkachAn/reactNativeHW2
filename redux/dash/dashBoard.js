import {createSlice} from "@reduxjs/toolkit";
//
export const dashSlice = createSlice({
  name: "dashboard",
  initialState: {
    photoId: null,
    name: null,
    ref: null,
  },
  reducers: {},
});
